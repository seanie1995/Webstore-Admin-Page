import React from "react";
import CreateForm from "@/components/form-components/create-form";

const CreateProduct = () => {
  return (
    <div className="gap-4 fixed inset-0 flex items-center justify-center z-50 bg-gray-800">
      <CreateForm />
    </div>
  );
};

export default CreateProduct;
